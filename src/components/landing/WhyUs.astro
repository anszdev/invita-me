---
import TicketHorizontal from "./TicketHorizontal.astro";

import Container from "@common/Container.astro";
import Question from "@assets/question.svg";
import Devices from "@assets/devices.svg";
import Design from "@assets/design.svg";
import Sheet from "@assets/sheet.svg";
import Cursor from "@assets/cursor.svg";
---

<section class="py-28">
  <Container>
    <div class="text-center max-w-4xl mx-auto">
      <h2
        class="font-semibold text-6xl relative flex items-center justify-center gap-4"
      >
        <Question
          class="size-36 rotate-[170deg] text-inv-tertiary/50 mask-[linear-gradient(transparent,white_30%)]"
        />
        Por qué 🪅Invita.me
        <Question
          class="size-24 rotate-[20deg] text-inv-tertiary/50 mask-[linear-gradient(white_30%,transparent)]"
        />
      </h2>
      <h3 class="font-bold text-4xl mt-4">
        La forma moderna, creativa y consciente de invitar.
      </h3>
      <p class="font-medium text-xl mt-7 text-pretty">
        Transformamos una simple invitación en una experiencia memorable.
        Descubre cómo hacemos que tu evento destaque desde el primer clic.
      </p>
    </div>

    <div class="mt-16 relative max-w-6xl mx-auto w-full h-60">
      <div
        class="absolute inset-0 -z-10 w-3/4 h-[30rem] from-inv-primary/25 via-inv-primary/15 to-inv-primary/10 blur-3xl bg-radial -translate-x-1/2"
      >
      </div>
      <TicketHorizontal
        title="100% Digital y siempre contigo"
        description="Accede, comparte y actualiza tu invitación desde cualquier lugar y dispositivo. Sin papel, sin complicaciones."
        class="absolute bg-inv-primary -rotate-2 top-12 z-[3] text-inv-text-light"
      >
        <Devices class="size-16" slot="icon" />
      </TicketHorizontal>
      <TicketHorizontal
        title="Diseños que cuentan tu historia"
        description="Personaliza cada detalle o déjanos ayudarte a crear algo único. Nos inspiras tú, tu evento y tus ideas."
        class="bg-[#be8bff] absolute left-1/2 -translate-x-1/2 z-[2] text-inv-text-light"
      >
        <Design class="size-16" slot="icon" />
      </TicketHorizontal>
      <TicketHorizontal
        title="Sostenible y además responsable"
        description="Cuidamos el planeta contigo. Invitaciones digitales que reducen el uso de papel sin perder elegancia."
        class="bg-[#e3cdff] absolute right-0 rotate-2 top-12 z-[1] text-inv-text-dark"
      >
        <Sheet class="size-16" slot="icon" />
      </TicketHorizontal>
    </div>
    <div class="flex items-center gap-2 mt-16 justify-center">
      <Cursor class="size-10" />
      <p class="font-medium text-xl">Pasa el cursor</p>
    </div>
  </Container>
</section>
<script>
  const $cards: NodeListOf<HTMLDivElement> = document.querySelectorAll(
    "[data-card='horizontal']"
  );
  let activeCard: number = 0;

  $cards.forEach((card, index) => {
    card.addEventListener("mouseenter", (e: Event) => {
      if (activeCard !== index) {
        $cards[activeCard].classList.remove("-translate-y-3");
        $cards[activeCard].style.zIndex = "";
      }
      activeCard = index;
      $cards[activeCard].classList.add("-translate-y-3");
      $cards[activeCard].style.zIndex = "5";
    });
  });
</script>
