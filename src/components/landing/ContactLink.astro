---
import Arrow from "@assets/arrow.svg";

type Props = {
  href: string;
};

const { href } = Astro.props;
---

<a
  href={href}
  class="flex justify-between items-center text-inv-secondary hover:text-inv-ui-accent group transition-colors ease-in-out duration-300"
>
  <slot />
  <div class="basis-3/12 flex items-center justify-between">
    <span
      class="size-44 opacity-0 group-hover:opacity-100 transition-all ease-in-out duration-300 rotate-45 group-hover:rotate-0"
    >
      <slot name="icon" />
    </span>
    <span class="p-2 bg-inv-tertiary/50 rounded-full">
      <Arrow
        class="size-24 -rotate-45 group-hover:rotate-0 transition-transform duration-300 ease-in-out"
      />
    </span>
  </div>
</a>
